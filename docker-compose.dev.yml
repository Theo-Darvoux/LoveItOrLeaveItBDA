services:
  dev:
    image: node:20-alpine
    container_name: love-it-leave-it-dev
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
