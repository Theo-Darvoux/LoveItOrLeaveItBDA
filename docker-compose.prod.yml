services:
  prod:
    image: ${DOCKER_IMAGE:-love-it-leave-it:latest}
    build: .
    container_name: love-it-leave-it-prod
    ports:
      - "9001:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
